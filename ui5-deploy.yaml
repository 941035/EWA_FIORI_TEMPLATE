# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.5"
metadata:
  name: com.parker.ewaformtemp
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: DS1
          url: http://parkerds1vm:8001
        app:
          name: ZEWAFORMTEMP
          description: EWA Form Template
          package: ZFIORIP
          transport: DS1K900030
